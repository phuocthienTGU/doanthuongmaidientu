Frontend usage

- Ensure backend is running at http://localhost:5000
- Open pages via backend (not file://), e.g. http://localhost:5000/index.html
- To change image base URL, edit `public/img-config.js` and set `window.IMG_BASE`.
